#include "main.h"

/**
 * mul - prints the product of 2 numbers
 * @a: first number
 * @b: second number
 * Return: the value of the product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
